export const config = {
  API_URI: 'https://swapi.dev/api/',
  API_KEY: '',
  API_IMG: '',
  MAP_API_KEY: '',
  SIZE_MATTERS_BASE_WIDTH: null,
  SIZE_MATTERS_BASE_HEIGHT: null,
};

export const TAB_BAR_HEIGHT: (arg: number) => number = (bottom: number) => (bottom ? 70 : 50);
export const HEADER_HEIGHT = 85;
